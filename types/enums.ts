export enum FormInputType {
  INPUT = "input",
  CHECKBOX = "checkbox",
  RADIO = "radio",
  TEXTAREA = "textarea",
  SELECT = "select",
  DATEPICKER = "datepicker",
  PHONE = "phone",
  SKELETON = "skeleton",
}
